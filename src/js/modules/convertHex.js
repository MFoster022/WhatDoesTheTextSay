function ConvertHex(userText){
    var userInput = document.getElementById(userText).value;

    if (userInput == ""){
        alert("Please enter some information into the box.")
    }else {
        var hexOutput = userInput.toString(16);
        let hexOutput;
        return hexOutput;
    }
}

